<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center" >

    <ProgressBar
        android:id="@+id/download_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true" />

    <ImageView
        android:id="@+id/download_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/download_progress"
        android:layout_alignParentLeft="true"
        android:src="@drawable/download_database" />

    <TextView
        android:id="@+id/txt_download_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/download_progress"
        android:layout_alignParentRight="true"
        android:text="即将开始"
        android:textColor="@color/black" />

    <TextView
        android:id="@+id/txt_download_dictionary_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/download_icon"
        android:layout_toRightOf="@+id/download_icon"
        android:text="字典"
        android:textColor="@color/black" />

</RelativeLayout>